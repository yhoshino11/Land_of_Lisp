; (defun half (n)
  ; (/ n 2))
; (half 2)
; returns 1

; #'half
; returns #<FUNCTION HALF (N)...>

; (lambda (n) (/ n 2))
; returns #<FUNCTION :LAMBDA (N) (/ N 2)>

; (mapcar (lambda (n) (/ n 2))
        ; '(2 4 6))
; returns (1 2 3)
